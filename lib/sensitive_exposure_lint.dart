/// The `@Sensitive` annotation for the `sensitive_exposure_lint` plugin.
///
/// Add `sensitive_exposure_lint` to `dependencies` to use the annotation, and
/// to the top-level `plugins` section of `analysis_options.yaml` to enable the
/// rule that acts on it.
library;

export 'package:sensitive_exposure_lint/src/annotations.dart';
