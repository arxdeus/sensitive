/// The `@Sensitive` annotation for the `sensitive` plugin.
///
/// Add `sensitive` to `dependencies` to use the annotation, and
/// to the top-level `plugins` section of `analysis_options.yaml` to enable the
/// rule that acts on it.
library;

export 'package:sensitive/src/annotations.dart';
